server {
    server_name ember.local;
    root /var/htdocs/ember/web/public;
    error_log /var/log/nginx/ember.error.log;

    index index.html index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    include includes/php_index;
}
